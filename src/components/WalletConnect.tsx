import { PhantomButton } from "@/components/PhantomButton";

/** Alias komponen koneksi wallet Solana (Phantom) untuk dipakai di halaman mana pun. */
export function WalletConnect() {
  return <PhantomButton />;
}
